import { floorTile, redTile, wallTile, livingTiles } from "./groundTiles";

import { livingTileColors } from "./colors";

module.exports = {
  wallTile,
  redTile,
  floorTile,
  livingTiles,
  livingTileColors,
};
