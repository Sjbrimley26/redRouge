import { get2DContext, getCanvas } from "./general";

import {
  drawScreen,
  resizeCanvas,
  renderSprite
} from "./draw";

module.exports = {
  get2DContext,
  getCanvas,
  resizeCanvas,
  drawScreen,
  renderSprite
};