import { get2DContext, getCanvas } from "./general";

import {
  drawScreen,
  resizeCanvas,
  renderSprite,
  renderMultipleSprites
} from "./draw";

module.exports = {
  get2DContext,
  getCanvas,
  resizeCanvas,
  drawScreen,
  renderSprite,
  renderMultipleSprites
};